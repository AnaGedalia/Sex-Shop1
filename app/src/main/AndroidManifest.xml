<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wsp.utsjr.com.wishofsexproducts" >
<!--En el siguiente Layout se maneja el estilo de la interfaz, lo que viene siendo la pestaña donde
se muestra su logo, nombre de la app y asignamos un tema -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_sex"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
